## Overview
In this lab, we generate a table of temperatures.

| | |
| --------- | --------------------------- |
| Exercise Folder | ForLoop |
| Builds On | WhileLoop | 
| Time to complete | 10 minutes |

## Success Criteria
Your solution should:
- Replace while loop with equivalent for loop
- Remove unnecessary variable declarations
- Produce same tabular output as WhileLoop version
- Correctly format temperature conversions
- Bonus: Display only even Fahrenheit temperatures

## Where to find the solution
See [solutions/ForLoop](../../solutions/ForLoop)

---

- Convert the **while** to a **for** in the previous lab.

1. Delete or comment out the declaration of `F` and any other variables that are not needed.
1. Delete or comment out the `while` statement.
1. Replace the `while` with a `for` statement.
    * `for (init; condition; increment)`
1. Remove the increment statement at the bottom of the loop

- Challenge: *Convert only even F temperatures*

### Success criteria
- The `for` loop correctly replaces the `while` loop.
- Output matches the WhileLoop lab’s format and range.
- Challenge: Only even Fahrenheit values are processed when implemented.

### Where to find the solution
For reference, see the completed example in:
- solutions/ForLoop/ForLoop.csproj

---
